FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    fastapi \
    uvicorn \
    onnxruntime \
    tokenizers \
    numpy

COPY embedding/embedding_server.py .

EXPOSE 8000

CMD ["uvicorn", "embedding_server:app", "--host", "0.0.0.0", "--port", "8000"]
